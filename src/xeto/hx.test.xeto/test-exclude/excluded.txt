This file must never be packaged into the xetolib.

Packaging is opt-in: the lib.xeto pragma selects files via its include and
publish patterns, and nothing selects this dir.  NamespaceTest and ZipLibTest
both verify it is absent from the lib files.
